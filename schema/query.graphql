query FetchPosts {
  posts {
    title
    link
    author {
      name
    }
  }
}

query Me {
  me {
    name
  }
}

mutation Login($name: String!) {
  login(name: $name) {
    user {
      name
    }
    token
  }
}
